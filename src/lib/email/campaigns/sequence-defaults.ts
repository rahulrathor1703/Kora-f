export const DEFAULT_INITIAL_OUTREACH_BODY = `Hi {{first_name}},

I noticed {{company}} is...

Would love to connect.

Best,
{{sender_name}}`;

export const DEFAULT_FOLLOW_UP_SUBJECT =
  'Re: Quick question about {{company}}';

export const DEFAULT_FOLLOW_UP_BODY = `Hi {{first_name}},

Just bumping this in case it got buried.

Best,
{{sender_name}}`;

export const DEFAULT_FOLLOW_UP_DELAY_DAYS = 3;
